# دليل إعداد وتشغيل تطبيق سوق سوريا الشامل

## المتطلبات
- تثبيت Flutter SDK (الإصدار 3.0.0 أو أحدث)
- تثبيت Android Studio أو VS Code
- تثبيت Dart plugin

## خطوات التشغيل

### 1. تثبيت الحزم
```bash
flutter pub get
```

### 2. إعداد ملف .env
ملف `.env` موجود وجاهز. يحتوي على:
- `SUPABASE_URL` - رابط قاعدة البيانات
- `SUPABASE_ANON_KEY` - مفتاح الوصول

### 3. تشغيل التطبيق
```bash
flutter run
```

## كيفية جعل نفسك مديراً

بعد إنشاء حساب في التطبيق، يجب عليك تعيين نفسك كمدير من قاعدة البيانات:

1. اذهب إلى لوحة تحكم Supabase
2. افتح SQL Editor
3. نفذ هذا الأمر (استبدل البريد الإلكتروني ببريدك):

```sql
UPDATE profiles SET is_admin = true WHERE id = (
  SELECT id FROM auth.users WHERE email = 'your-email@example.com'
);
```

## الميزات المضافة والمصلحة

### أمان
- نظام تسجيل دخول حقيقي (إيميل وكلمة مرور)
- حماية على قاعدة البيانات (RLS) - كل مستخدم يرى ويعدل إعلاناته فقط
- المدير فقط يستطيع الموافقة والرفض والتمويل
- لا يمكن للمستخدمين تغيير صلاحيات أنفسهم

### ميزات جديدة
- صفحة حساب شخصي تعرض إعلاناتي وحالتها (منشور/قيد المراجعة/مرفوض)
- مفضلة لحفظ الإعلانات المهمة
- بحث داخل كل قسم
- تصفية حسب المحافظة داخل الأقسام
- أزرار اتصال حقيقية (هاتف، واتساب، تليجرام)
- زر مشاركة حقيقي
- رفع صور حقيقي إلى Supabase Storage
- لوحة إدارة حقيقية مع موافقة/رفض/حذف

### بنية الكود
- مقسم إلى ملفات منطقية:
  - `lib/models/` - نماذج البيانات
  - `lib/services/` - خدمات قاعدة البيانات
  - `lib/screens/` - شاشات التطبيق
  - `lib/widgets/` - عناصر واجهة قابلة لإعادة الاستخدام
  - `lib/constants/` - الثوابت والأقسام

## ملاحظة مهمة
هذا الكود لم يُختبر على جهاز حقيقي. يجب عليك:
1. تشغيل `flutter pub get`
2. تشغيل `flutter run` على جهاز أو محاكي
3. اختبار جميع الميزات قبل النشر
